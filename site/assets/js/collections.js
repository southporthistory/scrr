const $=s=>document.querySelector(s),el=(t,c,x)=>{const n=document.createElement(t);if(c)n.className=c;if(x!=null)n.textContent=x;return n;},nfmt=n=>n.toLocaleString("en-US");
function card(c){const a=el("a","shelf");a.href=`archive.html?collection=${encodeURIComponent(c.id)}`;const im=el("div","shelf__img");im.append(c.cover?thumbImg(c.cover):placeholderEl());const b=el("div","shelf__body");b.append(el("h3",null,c.title));if(c.description)b.append(el("p",null,c.description));const meta=el("div","shelf__n");meta.append(el("span",null,`${nfmt(c.itemCount)} item${c.itemCount===1?"":"s"}`));b.append(meta);a.append(im,b);return a;}
(async()=>{const db=await(await fetch("data/catalog.json")).json(),roots=db.collections.filter(c=>!c.parentId),box=$("#groups");for(const root of roots){const children=db.collections.filter(c=>c.parentId===root.id);if(!children.length)continue;const sec=el("section","shelves"),head=el("div","shelves__head");head.append(el("h2",null,root.title),el("p",null,root.description));const grid=el("div","shelf-grid");for(const c of children)grid.append(card(c));sec.append(head,grid);box.append(sec);}})();
